version: '3.1'

services:
  PollerBox:
    restart: always
    image: bernhardpollerspoeck/pollerbox:latest
    privileged: true
    ports:
      - "8080:8080"
    volumes:
      - /sys:/sys
      - /etc:/etc
      - /dev:/dev
    user: root